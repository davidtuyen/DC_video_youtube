---
description: Diff changes and generate a PR description summary
---
1. Run `git diff --stat` to see file summary
2. Run `git diff HEAD` to capture detailed changes
3. Generate a Markdown summary of all changes for the Pull Request
