---
description: Search & Replace helper with immediate test verification
---
1. Perform search and replace for the target symbol/name
2. Run type checker (e.g. `tsc` or `mypy`) to catch interface mismatches
3. Run unit tests to ensure logic integrity
